# SCRIPT15 学习笔记：单根黄色试管抓取放置

对应脚本：`scripts/15_demo_single_tube.py`

脚本 15 是当前单试管 demo 主线：识别一根黄色试管，抓起来，把盖子转成朝上，
再按图像角度正负选择一套固定关节，把试管放到凹槽，最后回到六关节零位。

## 推荐运行命令

只做视觉预览，不连接机械臂：

```bash
python scripts/15_demo_single_tube.py \
  --serial 135122076361
```

只读检查 IK，不使能、不运动：

```bash
python scripts/15_demo_single_tube.py \
  --serial 135122076361 \
  --ip 10.42.0.101 \
  --check-ik
```

真实执行：

```bash
python scripts/15_demo_single_tube.py \
  --serial 135122076361 \
  --ip 10.42.0.101 \
  --execute
```

## 先从哪里读

建议先读文件底部的 `main()`。

它的顺序很直观：

1. 读取命令行参数；
2. 读取手眼标定；
3. 打开相机和 YOLO，按 `C` 锁定黄色试管；
4. 如果只是预览，就不连接机械臂；
5. 如果是 `--check-ik` 或 `--execute`，连接 CArm；
6. 检查机械臂是否接近六关节零位；
7. `build_cap_up_plan()` 只读生成动作计划；
8. `--execute` 时才调用 `execute_plan()` 真实运动。

## 最重要的固定参数

脚本开头的常量都是现场参数，读代码时先看这里。

- `CALIBRATION_DIR`：外部手眼标定目录；
- `TABLE_Z_ARM_MM`：工作台平面高度，单位 mm；
- `TOOL = 1`：使用夹爪 TCP；
- `GRASP_Z = 0.165`：抓取下降高度，单位 m；
- `APPROACH_Z_M = 0.350`：试管上方更高点，单位 m；
- `YELLOW_TUBE_POS_JOINTS`：图像角度为正时使用的四个固定关节；
- `YELLOW_TUBE_NEG_JOINTS`：图像角度为负时使用的四个固定关节；
- `ZERO_JOINTS`：六关节零位。

固定关节表的 4 行顺序是：

```text
第0行：盖子朝上
第1行：凹槽上方
第2行：凹槽释放
第3行：松爪后撤回
```

## 视觉部分在做什么

`detect_and_lock()` 负责实时识别黄色试管。

它会找：

- `y-body`：黄色管身；
- `y-cap`：黄色盖子。

然后 `yellow_geometry()` 从掩膜中算出：

- 中心点；
- B 点：无盖端；
- C 点：靠近黄色盖子的端点；
- 图像里的 B→C 角度。

按 `C` 锁定时，脚本会把像素点通过 `pixel_to_arm()` 转到机械臂基座坐标系，
得到机械臂要去抓的 XY 位置。

## 图像角度为什么重要

脚本现在不再对凹槽阶段做复杂 IK 搜索，而是采用现场标定过的固定关节。

选择规则很简单：

```text
图像角度 >= 0：用 YELLOW_TUBE_POS_JOINTS
图像角度 <  0：用 YELLOW_TUBE_NEG_JOINTS
```

这样做的好处是代码更简单，现场行为更可控。

代价是：如果相机摆放、凹槽位置、夹爪 TCP 或固定关节重新标定了，这两套关节也
要一起更新。

## IK 检查和真实执行的区别

`--check-ik` 会连接机械臂，但只做只读检查：

- 读取当前关节；
- 检查是否从零位开始；
- 计算准备位、试管上方位、抓取位是否有 IK；
- 打印选择了 POS 还是 NEG 分支。

它不会使能，也不会运动。

`--execute` 才会真实运动，而且需要输入完整确认文字。

## 真机动作顺序

`execute_plan()` 的动作顺序是：

1. 到高空准备位；
2. 到试管更高上方并张开夹爪；
3. 直线下降到抓取高度；
4. 闭爪；
5. 直线抬回更高上方；
6. 移动到固定盖朝上关节；
7. 确认盖子朝上；
8. 移动到固定凹槽高空位；
9. 移动到固定凹槽释放位；
10. 打开夹爪；
11. 移动到固定撤离关节位；
12. 回到六关节零位。

## 读这个脚本时最容易混淆的点

- 图像角度不是机械臂 yaw。图像角度只用来选 POS/NEG 固定关节分支。
- 机械臂 yaw 来自 `pixel_to_arm()` 后的 B→C 方向。
- `--check-ik` 不等于无碰撞，只说明关键目标位能算出 IK。
- 固定关节是现场标定值，不是通用算法自动算出来的。

