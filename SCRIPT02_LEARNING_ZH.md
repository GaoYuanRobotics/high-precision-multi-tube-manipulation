# 脚本02学习笔记：纯数学冒烟测试

对应文件：[`scripts_archive/02_smoke_test_project.py`](scripts_archive/02_smoke_test_project.py)

## 1. 这个脚本解决什么问题

用代码自己生成的小数据检查二维仿射、PCA 长轴方向和 CArm 返回值分类。

## 2. 输入、输出和副作用

- 输入：无需外部文件；测试点和细长点云都由脚本生成。
- 输出：全部检查通过时打印 `Project smoke test passed.`；失败时抛出 AssertionError。
- 是否接触硬件：请结合下面的运行模式和注意事项判断；没有 `--execute` 不代表所有视觉脚本都不会打开相机。

## 3. 主流程

1. 构造像素点和机械臂平面点
2. 用最小二乘拟合 2×3 仿射矩阵
3. 用 SVD/PCA 求细长点云主轴并让方向指向管盖
4. 检查常见 CArm 成功/失败字符串

## 4. 建议阅读顺序

- `fit_affine()`：求 `[x,y] = A @ [u,v,1]`
- `directed_long_axis()`：计算主轴并消除正负号歧义
- `assert_close()`：用容差比较浮点数
- `main()`：依次运行三个不接硬件的检查

初学者建议先读文件末尾的 `main()`，看清调用顺序后再回头阅读上面的辅助函数。

## 5. 本脚本涉及的 Python 语法

- `np.column_stack()` 按列拼矩阵
- `result[0]` 从最小二乘返回结果中取出参数矩阵
- 两层普通 `for` 循环生成规则点云
- 点积 `np.dot()` 用来判断两个单位向量方向是否一致

## 6. 数学或领域知识

仿射矩阵用最小二乘求解；PCA/SVD 的第一主方向是方差最大的方向。主轴天然有 ± 两个等价方向，所以还需要管盖位置决定箭头朝向。

## 7. 常用命令

\`\`\`bash
python scripts_archive/02_smoke_test_project.py
\`\`\`

## 8. 容易混淆的地方

冒烟测试只说明这些小型数学例子通过，不等于真实相机标定、真实模型和机械臂路径已经验证。

## 9. 学习检查题

- 这个脚本的输入数据来自哪里？
- 哪一个函数产生最终结果？
- 坐标、长度和角度分别使用什么单位？
- 发生异常时，文件、相机或机械臂连接在哪里释放？
- 哪些检查只能降低风险，不能证明真实路径绝对安全？
